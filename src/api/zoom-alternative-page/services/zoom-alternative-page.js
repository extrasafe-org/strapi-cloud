'use strict';

/**
 * zoom-alternative-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::zoom-alternative-page.zoom-alternative-page');
