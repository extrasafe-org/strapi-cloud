'use strict';

/**
 * zoom-alternative-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::zoom-alternative-page.zoom-alternative-page');
