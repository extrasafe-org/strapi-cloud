'use strict';

/**
 * signal-alternative-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::signal-alternative-page.signal-alternative-page');
